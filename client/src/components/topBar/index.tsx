import { Typography } from "@mui/material"

const TopBar = () => {
    return (
        <Typography align="center" sx={{
            fontSize: 30,
            fontWeight: "Bold",
            height: "10vh"
        }}>Jackpot</Typography>
    )
}

export default TopBar;
